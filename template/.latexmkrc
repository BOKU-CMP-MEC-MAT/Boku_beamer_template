@default_files = ('slides.tex');

# $pdf_previewer = 'evince %O %S';  # Uncomment/customize if you want a fixed previewer; otherwise latexmk picks an OS default

#$latex = 'latex -interaction=nonstopmode -shell-escape';
#$pdflatex = 'pdflatex -interaction=nonstopmode -shell-escape';

$clean_ext = "dvi nav snm vrb";
